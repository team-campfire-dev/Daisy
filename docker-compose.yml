version: '3'

services:
  app:
    container_name: daisy-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3001:3001
    env_file:
      - .env.local
    volumes:
      - ./public:/app/public
